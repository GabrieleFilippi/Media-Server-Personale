# === Media Server - Configurazione ===

# Percorso root del disco dedicato ai media
# Esempio: /mnt/media, /srv/media, /data
MEDIA_ROOT=/mnt/media

# Directory derivate (normalmente non serve modificarle)
DOWNLOADS_DIR=${MEDIA_ROOT}/downloads
MOVIES_DIR=${MEDIA_ROOT}/movies
TV_DIR=${MEDIA_ROOT}/tv

# Directory configurazioni dei servizi (relativa al progetto)
CONFIG_DIR=./config

# Utente e gruppo per i permessi dei container
# Trova i tuoi valori con: id
PUID=1000
PGID=1000

# Timezone
TZ=Europe/Rome

# === Accesso Esterno ===

# Indirizzo IP Tailscale del server (100.x.x.x)
# Trova il tuo IP con: tailscale ip -4
# Usato per esporre i servizi admin SOLO via Tailscale
TAILSCALE_IP=100.x.x.x

# Dominio pubblico per Jellyfin (es: jellyfin.tuodominio.com o tuonome.duckdns.org)
DOMAIN=jellyfin.example.com

# === DDNS (Dynamic DNS) ===
# Credenziali per il servizio DDNS usato da ddclient.
# Compilare in base al provider scelto; non inserire valori reali qui.
#
# --- DuckDNS ---
# DDNS_DUCKDNS_TOKEN=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# DDNS_SUBDOMAIN=tuonome   # solo la parte prima di .duckdns.org
#
# --- Cloudflare ---
# DDNS_CF_EMAIL=tua@email.com
# DDNS_CF_API_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# DDNS_CF_ZONE=tuodominio.com
# DDNS_CF_SUBDOMAIN=jellyfin.tuodominio.com
#
# Nota: queste variabili NON sono lette direttamente da docker-compose.yml.
# Vanno inserite manualmente in ${CONFIG_DIR}/ddclient/ddclient.conf.
